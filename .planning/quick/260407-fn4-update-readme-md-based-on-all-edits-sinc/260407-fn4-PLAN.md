---
phase: quick
plan: 260407-fn4
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README contains a Competition Scoring section explaining how to use score_competition.m"
    - "README Models table includes new BLDC and button-ctrl models"
    - "README Other Scripts table includes new scripts (sensitivity analysis, ZOH, BLDC accel)"
    - "README Hardware section mentions CRC in SPI communication"
  artifacts:
    - path: "README.md"
      provides: "Updated project documentation"
      contains: "Competition Scoring"
---

<objective>
Update README.md to reflect all changes since the last README commit (65f66e7):
new competition scoring script, new BLDC/stepper models, CRC in SPI comms,
and new analysis scripts.

Purpose: Keep project documentation accurate and add a usage guide for the
scoring tool that the instructor will use on competition day.

Output: Updated README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@scripts/score_competition.m (lines 1-50 for config, lines 256-300 for finalization)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with new content</name>
  <files>README.md</files>
  <action>
Read the current README.md (286 lines). Make the following edits, preserving the
existing structure and tone:

**1. Add new models to the Simulink Models table (line ~40-50)**

Insert these rows into the "Main RR Pendulum Models" table, keeping alphabetical/logical
order within the table:

| Model | Description |
|-------|-------------|
| `RRpendulum_digtwin_FSFB_BLDC_noUI.slx` | BLDC state feedback without UI (headless codegen) |
| `RRpendulum_digtwin_swingup_buttonctrl.slx` | Button-controlled swing-up (manual trigger via hardware button) |

Note: `RRpendulum_digtwin_BLDC.slx` and `RRpendulum_digtwin_FSFB_BLDC.slx` are already
in the table. The BLDC models live in `models/BLDC/`, stepper models in `models/stepper/`,
but the table currently lists them flat without folder prefixes -- keep that convention and
add a note in the Description column for the BLDC-specific ones.

Also note: `RRpendulum_digtwin_swingup2.slx` exists in stepper/ but is likely an iteration;
do NOT add it (no clear public purpose).

**2. Update Hardware Interface subsection (~line 52-53)**

After the existing sentence about SPI and milli-Nm, add a note:

"SPI frames include CRC error checking for robust communication with the latest pendulum firmware."

**3. Add rows to the Other Scripts table (~line 174-179)**

Add these rows to the "Other Scripts" table:

| Script | Purpose |
|--------|---------|
| `score_competition.m` | Competition scoring tool (see [Competition Scoring](#competition-scoring) below) |
| `RRpendulum_FSFB_controldesign_accel.m` | LQR design with acceleration-command interface (BLDC variant) |
| `RRpendulum_FSFB_sensitivity_analysis.m` | Sensitivity analysis of LQR gains to parameter variations |
| `analyze_ZOH_performance.m` | Zero-order hold discretization performance analysis |

Keep existing rows. The `score_competition.m` entry should cross-reference the new section.

**4. Add "Competition Scoring" section -- insert BEFORE the "## Requirements" section (~line 234)**

Add a new H2 section with this content (adapt formatting to match existing README style):

```
## Competition Scoring

`scripts/score_competition.m` is a MATLAB scoring tool for the digital twin swingup
competition. It processes team submissions (`.mldatx` files from Simulink Data Inspector),
validates successful swingups, computes swingup times and simulation accuracy (SMAPE),
and produces a ranked leaderboard with plots.

### Prerequisites

- MATLAB project open (`openProject('digtwin_labo.prj')` -- the script checks automatically)
- Team `.mldatx` files ready (each file contains one hardware run and one simulation run)

### Usage Workflow

1. **Run the script:**
   ```matlab
   run('scripts/score_competition.m')
   ```

2. **Select q2 display unit** (rev, deg, or rad) when prompted

3. **Session loop** (repeat for each attempt):
   - Select a `.mldatx` file via the file dialog
   - Assign it to a team (5 pre-configured teams)
   - Select the command signal and q2 signal from the file's signal list
   - Preview hw/sim overlay and confirm signal selection
   - Enter time alignment delta (left-shift hardware signal, in seconds)
   - Type `done` to finalize, or press Enter to load the next file

4. **Finalization** (automatic after `done`):
   - Per-team diagnostic summary (swingup times, SMAPE values)
   - Ranked leaderboard table
   - CSV, xlsx, and .mat export to `data/`
   - Score breakdown bar chart

### Configuration

Edit the `cfg` struct at the top of the script to adjust:

| Parameter | Default | Description |
|-----------|---------|-------------|
| `cfg.teams` | 5 teams (1 BLDC + 4 stepper) | Team names and motor types |
| `cfg.smape_fixed_duration` | 5 s | SMAPE comparison window duration |
| `cfg.swingup_hold_time` | 1.0 s | Time pendulum must hold upright |
| `cfg.swingup_tolerance_deg` | 2 deg | Tolerance around upright position |
| `cfg.time_points` | [2, 1, 0.5, 0] | Stepper time-ranking points (1st to 4th) |
| `cfg.smape_points` | [2, 1, 0.5, 0.5] | Stepper SMAPE-ranking points (1st to 4th) |
| `cfg.bldc_smape_bands` | [40, 80, 120, 160] | BLDC absolute SMAPE% band edges |
| `cfg.bldc_smape_pts` | [4, 3, 2, 1, 0] | Points per SMAPE band |
| `cfg.export_dir` | `data` | Output directory for results |

### Output Files

After finalization, results are saved to `data/` with timestamped filenames:

| File | Contents |
|------|----------|
| `competition_results_YYYYMMDD_HHMM.csv` | Leaderboard table (CSV) |
| `competition_results_YYYYMMDD_HHMM.xlsx` | Leaderboard table (Excel) |
| `competition_results_YYYYMMDD_HHMM.mat` | Full session struct and table for post-hoc analysis |

### Scoring Rules

- **Stepper teams** (4 teams): Ranked competitively on swingup time and SMAPE. Best result per metric across multiple attempts is used.
- **BLDC team** (1 team): Scored on absolute SMAPE bands (not ranked against stepper teams).
- **Participation point**: 1 point awarded if the pendulum exceeds 90 degrees during any attempt.
```

Make sure the anchor `competition-scoring` is created by the H2 heading naturally.
Do NOT change any other sections. Preserve all existing content exactly.
  </action>
  <verify>
    <automated>cd "C:/Users/u0130154/MATLAB/projects/digtwin_labo" && grep -c "Competition Scoring" README.md && grep -c "score_competition" README.md && grep -c "buttonctrl" README.md && grep -c "CRC" README.md && wc -l README.md</automated>
  </verify>
  <done>
    - README.md contains a new "Competition Scoring" H2 section with prerequisites, workflow, config table, outputs, and scoring rules
    - Models table includes RRpendulum_digtwin_FSFB_BLDC_noUI.slx and RRpendulum_digtwin_swingup_buttonctrl.slx
    - Other Scripts table includes score_competition.m, FSFB_controldesign_accel, sensitivity_analysis, and analyze_ZOH_performance
    - Hardware Interface subsection mentions CRC error checking
    - All existing content is preserved unchanged
    - Total line count is approximately 370-400 lines (was 286)
  </done>
</task>

</tasks>

<verification>
- `grep "## Competition Scoring" README.md` returns a match
- `grep "score_competition.m" README.md` returns matches in both the Other Scripts table and the Competition Scoring section
- `grep "buttonctrl" README.md` returns a match in the Models table
- `grep "CRC" README.md` returns a match in the Hardware Interface subsection
- `grep "sensitivity_analysis" README.md` returns a match in the Other Scripts table
- No existing content was removed or altered (spot-check key sections)
</verification>

<success_criteria>
README.md is updated with all four additions (scoring section, new models, CRC note, new scripts)
and preserves all existing content. The Competition Scoring section provides enough detail for
an instructor to use the tool without reading the source code.
</success_criteria>

<output>
No SUMMARY file needed for quick tasks. The updated README.md is the deliverable.
</output>
