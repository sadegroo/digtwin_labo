---
phase: 05-milestone-cleanup-and-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/score_competition.m
  - .planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md
  - .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md
  - .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md
  - .planning/phases/04-scoring-and-output/04-01-SUMMARY.md
  - .planning/phases/04-scoring-and-output/04-02-SUMMARY.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "cfg.smape_window legacy field is removed from score_competition.m and its purpose documented in a comment near cfg.smape_fixed_duration"
    - "Finalization Steps 2 and 3 each have try/catch wrappers with fprintf(2,...) error messages matching the pattern of Steps 1, 4, 5"
    - "All Phase 1, 2, 4 SUMMARY frontmatters include a requirements_completed field listing the requirement IDs satisfied by that plan"
  artifacts:
    - path: "scripts/score_competition.m"
      provides: "Cleaned cfg struct (no smape_window), try/catch on finalization Steps 2-3"
    - path: ".planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md"
      provides: "requirements_completed field with LOAD-04, LOAD-05, LOAD-06"
    - path: ".planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md"
      provides: "requirements_completed field with SIGM-01..03, ALGN-01, ALGN-02"
    - path: ".planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md"
      provides: "requirements_completed field with OUTP-03"
    - path: ".planning/phases/04-scoring-and-output/04-01-SUMMARY.md"
      provides: "requirements_satisfied renamed to requirements_completed"
    - path: ".planning/phases/04-scoring-and-output/04-02-SUMMARY.md"
      provides: "requirements_completed field with OUTP-02"
  key_links:
    - from: "scripts/score_competition.m finalization block"
      to: "compute_leaderboard and disp_leaderboard calls"
      via: "try/catch wrappers"
      pattern: "try\\s+.*compute_leaderboard|try\\s+.*disp_leaderboard"
---

<objective>
Close the 3 remaining tech debt items from the v1.0 milestone audit: remove the dead `cfg.smape_window` field from `score_competition.m`, add try/catch wrappers to finalization Steps 2-3 for consistency with Steps 1/4/5, and backfill `requirements_completed` frontmatter in Phase 1/2/4 SUMMARY files.

Purpose: Achieve a clean milestone close where all documentation artifacts match the implementation state and error handling is consistent.
Output: Updated `scripts/score_competition.m` and 5 updated SUMMARY markdown files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean cfg.smape_window and add try/catch to finalization Steps 2-3</name>
  <files>scripts/score_competition.m</files>
  <read_first>
    - scripts/score_competition.m (lines 18-25 for cfg block, lines 260-295 for finalization section)
    - .planning/v1.0-MILESTONE-AUDIT.md (tech_debt section for exact items)
  </read_first>
  <action>
Two changes to scripts/score_competition.m:

**Change 1 — Remove cfg.smape_window (SC3):**
Delete line 21 (`cfg.smape_window = 'fixed'; % legacy; Phase 3 uses D-09 hybrid: max(5s, t_to_90deg)`).
Update the comment on the `cfg.smape_fixed_duration` line (currently line 22, will become line 21 after deletion) to read:
```
cfg.smape_fixed_duration   = 5;         % seconds — D-09 hybrid window: max(this, t_to_first_90deg)
```

Also verify that `cfg.smape_window` is not referenced anywhere else in the file. The audit confirmed `compute_metrics` never reads it (it applies D-09 unconditionally), so removal is safe.

**Change 2 — Wrap Steps 2-3 in try/catch (SC4):**
Currently lines 272-276 read:
```matlab
%[text] **Step 2:** Compute leaderboard table (SCOR-01..06, OUTP-01)
T = compute_leaderboard(session, cfg);

%[text] **Step 3:** Display leaderboard (D-10, D-11)
disp_leaderboard(T, session, cfg);
```

Replace with:
```matlab
%[text] **Step 2:** Compute leaderboard table (SCOR-01..06, OUTP-01)
try
    T = compute_leaderboard(session, cfg);
catch e
    fprintf(2, 'Step 2 error (leaderboard): %s\n', e.message);
    T = table();
end

%[text] **Step 3:** Display leaderboard (D-10, D-11)
try
    disp_leaderboard(T, session, cfg);
catch e
    fprintf(2, 'Step 3 error (display): %s\n', e.message);
end
```

Note: Step 2 assigns `T = table()` in its catch block so that Steps 4-5 still receive a valid (empty) table variable and do not error on an undefined `T`.
  </action>
  <verify>
    <automated>cd "C:/Users/u0130154/MATLAB/projects/digtwin_labo" && grep -c "cfg.smape_window" scripts/score_competition.m | grep "^0$" && grep -c "D-09 hybrid window" scripts/score_competition.m | grep "^1$" && grep -c "Step 2 error" scripts/score_competition.m | grep "^1$" && grep -c "Step 3 error" scripts/score_competition.m | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "cfg.smape_window" scripts/score_competition.m` returns zero matches (field removed)
    - `grep "D-09 hybrid window" scripts/score_competition.m` returns exactly 1 match (on the smape_fixed_duration line)
    - `grep "Step 2 error (leaderboard)" scripts/score_competition.m` returns exactly 1 match
    - `grep "Step 3 error (display)" scripts/score_competition.m` returns exactly 1 match
    - `grep "T = table()" scripts/score_competition.m` returns at least 1 match (fallback in Step 2 catch)
    - The finalization section has 5 try/catch blocks total (Steps 1-5), verified by `grep -c "^try$" scripts/score_competition.m` returning at least 5
  </acceptance_criteria>
  <done>cfg.smape_window field is gone, smape_fixed_duration comment documents D-09 hybrid, and all 5 finalization steps have consistent try/catch error handling</done>
</task>

<task type="auto">
  <name>Task 2: Backfill requirements_completed frontmatter in Phase 1, 2, 4 SUMMARYs</name>
  <files>
    .planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md
    .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md
    .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md
    .planning/phases/04-scoring-and-output/04-01-SUMMARY.md
    .planning/phases/04-scoring-and-output/04-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md
    - .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md
    - .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md
    - .planning/phases/04-scoring-and-output/04-01-SUMMARY.md
    - .planning/phases/04-scoring-and-output/04-02-SUMMARY.md
    - .planning/ROADMAP.md (phase requirement assignments)
    - .planning/phases/01-sdi-loading-and-session-loop/01-01-SUMMARY.md (line 46 for field format example)
    - .planning/phases/03-metric-computation/03-01-SUMMARY.md (line 46 for field format example)
  </read_first>
  <action>
Add or fix the `requirements_completed` frontmatter field in 5 SUMMARY files. Use the hyphenated YAML key format `requirements-completed` to match the existing convention in 01-01-SUMMARY.md and 03-01-SUMMARY.md.

**01-02-SUMMARY.md:** Add `requirements-completed: [LOAD-04, LOAD-05, LOAD-06]` after the existing `key-decisions:` block (before the `# Metrics` comment). These are the requirements satisfied by the session loop (LOAD-04: incremental session loop, LOAD-05: session state accumulation, LOAD-06: finalize command).

**02-01-SUMMARY.md:** Add `requirements-completed: [SIGM-01, SIGM-02, SIGM-03, ALGN-01, ALGN-02]` after the `metrics:` block (before the closing `---`). These are the requirements satisfied by the signal selection and alignment functions.

**02-02-SUMMARY.md:** Add `requirements-completed: [OUTP-03]` after the `metrics:` block (before the closing `---`). OUTP-03 (overlay plot after each file) is satisfied by the overlay figure wired into the session loop.

**04-01-SUMMARY.md:** Rename the existing `requirements_satisfied` key (line 43) to `requirements_completed` for consistency. The values stay the same: `[SCOR-01, SCOR-02, SCOR-03, SCOR-04, SCOR-05, SCOR-06, OUTP-01, OUTP-04, OUTP-05]`. Use underscored format `requirements_completed` since 04-01 uses underscored YAML keys throughout its frontmatter (unlike Phase 1/3 which use hyphens).

**04-02-SUMMARY.md:** Add `requirements_completed: [OUTP-02]` after the `files_modified` field in the metrics block (before the closing `---`). OUTP-02 (CSV and xlsx export) is satisfied by the export_results function. Use underscored format to match 04-01's convention.
  </action>
  <verify>
    <automated>cd "C:/Users/u0130154/MATLAB/projects/digtwin_labo" && grep -c "requirements.completed.*LOAD-04" .planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md | grep "^1$" && grep -c "requirements.completed.*SIGM-01" .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md | grep "^1$" && grep -c "requirements.completed.*OUTP-03" .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md | grep "^1$" && grep -c "requirements_completed" .planning/phases/04-scoring-and-output/04-01-SUMMARY.md | grep "^1$" && grep -c "requirements_completed.*OUTP-02" .planning/phases/04-scoring-and-output/04-02-SUMMARY.md | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "requirements-completed.*LOAD-04.*LOAD-05.*LOAD-06" .planning/phases/01-sdi-loading-and-session-loop/01-02-SUMMARY.md` returns 1 match
    - `grep "requirements-completed.*SIGM-01.*SIGM-02.*SIGM-03.*ALGN-01.*ALGN-02" .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-01-SUMMARY.md` returns 1 match
    - `grep "requirements-completed.*OUTP-03" .planning/phases/02-signal-selection-alignment-and-per-file-plots/02-02-SUMMARY.md` returns 1 match
    - `grep "requirements_completed" .planning/phases/04-scoring-and-output/04-01-SUMMARY.md` returns 1 match AND `grep "requirements_satisfied" .planning/phases/04-scoring-and-output/04-01-SUMMARY.md` returns 0 matches (old key removed)
    - `grep "requirements_completed.*OUTP-02" .planning/phases/04-scoring-and-output/04-02-SUMMARY.md` returns 1 match
    - Every Phase 1, 2, 4 SUMMARY file has a `requirements.completed` field (verified by grep across all 6 files returning 6 matches total: 01-01 already has it + 5 newly added)
  </acceptance_criteria>
  <done>All Phase 1, 2, and 4 SUMMARY files have requirements_completed frontmatter listing the exact requirement IDs each plan satisfied, consistent with the existing format in 01-01 and 03-01 SUMMARYs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. All changes are to local documentation files and a MATLAB script that only runs in an interactive session.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | score_competition.m Step 2 catch | accept | Step 2 catch sets T=table() so downstream steps receive a valid variable; worst case: empty leaderboard is exported |
</threat_model>

<verification>
1. `grep -c "cfg.smape_window" scripts/score_competition.m` returns 0
2. `grep -c "try" scripts/score_competition.m` returns at least 5 (Steps 1-5 in finalization, plus xlsx try/catch inside export_results)
3. All 6 Phase 1/2/4 SUMMARY files contain a `requirements.completed` line (01-01 pre-existing + 5 new)
4. MATLAB static analysis: `checkcode scripts/score_competition.m` returns no new errors from the edits
</verification>

<success_criteria>
- cfg.smape_window field removed from score_competition.m; smape_fixed_duration comment documents D-09 hybrid
- All 5 finalization steps (1-5) have symmetric try/catch wrappers with fprintf(2,...) error messages
- All Phase 1, 2, 4 SUMMARY frontmatters include requirements_completed with correct requirement IDs
- No regressions: script structure and Live Script format preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup-and-tech-debt/05-01-SUMMARY.md`
</output>
